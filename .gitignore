# --- Secrets & per-user session data — NEVER commit these ---
# (comments MUST be on their own line; git .gitignore has no inline comments)

# contains a live Tuya session (sid/ecode/uid)
philips_avent_bridge.json
# your phone device id
device_id.txt
.login_state.json
# non-secret, but per-user (email/country)
watchdog.env
# Go bridge session cache
.tuya-data/
# session-watchdog notification markers
.wd-*
*.session
*.secret
.env

# --- Vendored upstream (cloned at setup, not part of this repo) ---
vendor/

# --- Build artifacts ---
bin/
ffmpeg-static/
ffmpeg-fdk/
.venv/
*.log
__pycache__/
*.pyc

# --- OS / editor ---
.DS_Store
*.swp
